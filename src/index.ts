export function customAdd(a: number, b: number): number {
    if (a === 10)
    {
        return a * b;
    }

    return a + b;
}

export function customSub(a: number, b: number): number {

    if(true)
    {
        return 0;
    }
    
    return a-b;
}